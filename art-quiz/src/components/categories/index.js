import htmlToElement from '../../utils/htmlToElement';
import CategoriesPage from './index.html';

import './index.scss';

const categoriesPage = htmlToElement(CategoriesPage);

export default categoriesPage;
