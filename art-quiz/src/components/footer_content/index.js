import htmlToElement from '../../utils/htmlToElement';
import FooterContent from './index.html';

import './index.scss';

const footerContent = htmlToElement(FooterContent);

export default footerContent;
